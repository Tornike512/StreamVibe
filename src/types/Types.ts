export interface TGetMovies {
  id: string;
  genre: string;
  image: string;
}

export interface TGetStreams {
  id: string;
  streamTitle: string;
  image: string;
}

export interface TGetFaq {
  id: string;
  faq: string;
  faqNum: string;
}

export interface TSlideTypes {
  leftSlide: number;
  rightSlide: number;
}
